# Copie para .env.local e preencha com os valores reais.

# URL pública final do site (sem barra no fim). Usada em OG/WhatsApp/sitemap.
# Domínio: tardinhariosolimões.com (punycode por causa do acento).
# O código já usa este domínio por padrão; defina só se quiser sobrescrever
# (ex.: http://localhost:3000 durante o desenvolvimento).
NEXT_PUBLIC_SITE_URL=https://xn--tardinhariosolimes-82b.com

# WhatsApp para ingressos e patrocínios, formato internacional só com dígitos.
NEXT_PUBLIC_WHATSAPP=559282237716

# Analytics opcional (Plausible). Deixe vazio para desativar.
NEXT_PUBLIC_PLAUSIBLE_DOMAIN=
